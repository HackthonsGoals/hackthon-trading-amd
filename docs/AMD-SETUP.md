# AMD Developer Cloud / ROCm Setup (TODO)

This document will describe how to run the app on AMD Developer Cloud with ROCm-enabled PyTorch and collect real GPU benchmarks.

- [ ] Spin up ROCm-enabled instance (e.g., MI300X)
- [ ] Install ROCm drivers and PyTorch for ROCm
- [ ] Clone the repository
- [ ] Run `scripts/profile_gpu.py` to verify GPU tracing
- [ ] Start the Streamlit app and capture real benchmarks
